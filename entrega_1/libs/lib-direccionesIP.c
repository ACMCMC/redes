#include <stdlib.h>
#include <stdio.h>

void get_host_info(char* host_name) {
    printf("%s\n", host_name);
}

void get_service_info(char* service) {
    printf("%s\n", service);
}

void get_addr_info(char* addr) {
    printf("%s\n", addr);
}

void get_port_info(char* port) {
    printf("%s\n", port);
}